export default function Home() {
  return (
    <main className="mx-auto max-w-3xl p-6">
      <h1 className="text-3xl font-semibold">
        Weekly Planner — <span className="text-emerald-600">MVP</span>
      </h1>
      <p className="mt-4 text-gray-600">
        If you can read this and the green text shows, Next.js + Tailwind is working.
      </p>
      <p className="mt-2">Next step: we’ll add the form and preview in later milestones.</p>
    </main>
  );
}
